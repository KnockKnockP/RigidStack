using UnityEngine;

public class randomBackgroundObjectInformationHolder : MonoBehaviour {
    //A variable for determining the random object's lifetime.
    public float secondsToExistInTheScene = 0f;
}